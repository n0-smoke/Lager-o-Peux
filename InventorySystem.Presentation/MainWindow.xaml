<Window x:Class="InventorySystem.Presentation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dashboard" Height="300" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Welcome Header -->
        <TextBlock x:Name="WelcomeText"
                   FontSize="16"
                   FontWeight="Bold"
                   Margin="0,0,0,20" />

        <!-- Service Buttons -->
        <StackPanel Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Center" Orientation="Vertical" Width="200">
            <Button Content="Inventory Management" Click="OpenInventory_Click" Margin="0,0,0,10"/>
            <Button Content="Shipment Management" Click="OpenShipment_Click" Margin="0,0,0,10"/>
            <Button Content="Task Assignment" Click="OpenTasks_Click" Margin="0,0,0,10"/>
            <Button Content="Analytics Dashboard" Click="OpenAnalytics_Click"/>
            <Button Content="Register New User" Click="RegisterUser_Click" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</Window>
