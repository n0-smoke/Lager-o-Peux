<Window x:Class="InventorySystem.Presentation.EditTruckWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Truck" Height="250" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Identifier -->
        <Label Content="Identifier:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
        <TextBox x:Name="IdentifierBox" Grid.Row="0" Grid.Column="1" Margin="10,5"/>

        <!-- Capacity -->
        <Label Content="Max Capacity (kg):" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
        <TextBox x:Name="CapacityBox" Grid.Row="1" Grid.Column="1" Margin="10,5"/>

        <!-- Save Button -->
        <Button Content="Save"
                Grid.Row="2"
                Grid.Column="1"
                HorizontalAlignment="Right"
                Width="80"
                Margin="10,15,0,0"
                Click="Save_Click"/>
    </Grid>
</Window>
