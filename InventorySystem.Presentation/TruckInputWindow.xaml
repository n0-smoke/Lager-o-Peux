<Window x:Class="InventorySystem.Presentation.TruckInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Truck" Height="250" Width="420">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <Label Content="License Plate:"/>
            <TextBox x:Name="LicensePlateTextBox" />

            <Label Content="Model:" Margin="0,10,0,0"/>
            <TextBox x:Name="ModelTextBox" />

            <Label Content="Name:" Margin="0,10,0,0"/>
            <TextBox x:Name="NameTextBox" />
            <Button Content="Add" Width="80" Click="AddButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
